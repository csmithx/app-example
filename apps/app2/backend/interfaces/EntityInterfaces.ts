// @NOTICE The interface should be used only internally in the app.
// This interface represents the data from the database.

export interface SaleEntityInterface {
    id: string;
    user_id: string;
}